# React Native Performance Analysis Report

<!-- CANONICAL TEMPLATE — generated by react-native-performance-analyzer v0.0.1.
     Fill every section. Keep section order and headings EXACTLY as-is so the
     output structure is identical across apps, tools, and execution modes.
     Replace all {placeholders}. Delete the <-- hints -->.
     Example rows are shown for the Issues and Validation tables — copy the shape. -->

> **Mode:** {Analyze Only | Analyze & Fix | Fix & Report}
> **Project:** {App/Project name}
> **RN version:** {x.y.z} · **Expo SDK:** {x.y.z | none}
> **Platforms:** {iOS | Android | both}
> **Scope:** {screens/actions analyzed}
> **Build used for observations:** {Debug | Release | Mixed — release preferred for verdicts}
> **Generated by:** react-native-performance-analyzer v0.0.1 · **Date:** {YYYY-MM-DD}

> **Mode statement:** Mode: {Analyze Only | Analyze & Fix | Fix & Report} — {no code was changed. | fixes proposed for approval. | changes were applied per the approved permission grant.}

## Summary

{2–4 sentences: what was reviewed, the single real bottleneck (named), overall severity, and — if fixes were applied — the headline result.}

## Issues Found

| # | Area | Issue (traced to code) | Severity |
|---|------|------------------------|----------|
| 1 | {Startup/Rendering/Lists/Images/State/Navigation/Animations/Memory/Native/Hermes/Bundle} | {`file:line` → concrete, named cause} | {Critical/High/Medium/Low} |
| 2 | Lists | {`src/components/FeedCard.tsx:12` — inline `renderItem` creates a new function every render, defeating row bailout} | High |
| 3 | {Area} | {cause} | {Severity} |

<!-- Rule: every issue must trace to specific code. No "app is slow" — say which call, prop, or listener. -->

## Recommended Fixes

<!-- Mode 1: recommended only, never applied. Modes 2/3: this is the approved plan. -->

### Issue {1} — {short name} ({Severity})

- **Fix:** {surgical change; e.g. "stabilize `renderItem` with `useCallback`, memoize `FeedCard` with `React.memo`"}
- **Files affected:** {`src/...`}
- **Risk:** {Low/Medium/High} — {behavior impact}
- **Depends on:** {nothing | a measurement that justifies it, e.g. FlashList}
- **Validation:** {how to confirm: `npm run typecheck`, release-build re-measure, profiler trace}

### Issue {2} — {short name} ({Severity})

- **Fix:** {…}
- **Files affected:** {…}
- **Risk:** {…}

<!-- One block per issue. Do NOT combine issues into one block. -->

## Validation Notes

| Check | Command | Result |
|-------|---------|--------|
| TypeScript | `npm run typecheck` (or `tsc --noEmit`) | {✅ pass | ⚠️ unverified until run} |
| Lint | `npm run lint` | {…} |
| Tests | `npm test` | {…} |
| Android release | `cd android && ./gradlew assembleRelease` | {…} |
| iOS | `cd ios && pod install` (+ Release scheme) | {…} |
| Profiler | {React DevTools / Hermes / Android Studio / Instruments} | {…} |
| {Custom check} | {exact command} | {…} |

<!-- Any check not run MUST be listed with the exact command and marked "unverified until run". -->

## Manual Testing Steps

1. {Reproduce step — screen, action, device/build}
2. {Verify step — what "fixed" looks like}
3. {Regression step — business behavior unchanged}
4. {Low-end device check if applicable}

## Remaining Risks

- {Needs real-device / production-monitoring verification}
- {Assumption that could break the fix, e.g. variable row heights if `getItemLayout` was added}

## Next Steps

1. {What to do next; for Mode 1: "run Mode 2/3 with this report"}

---

_End of report — react-native-performance-analyzer v0.0.1_

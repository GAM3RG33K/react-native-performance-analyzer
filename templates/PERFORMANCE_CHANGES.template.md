# React Native Performance Changes Report

<!-- CANONICAL TEMPLATE — generated by react-native-performance-analyzer v0.0.1.
     Only used in Mode 3 (Fix & Report). Keep section order and headings EXACTLY
     as-is so the output structure is identical across apps and tools.
     Replace all {placeholders}. Delete the <-- hints -->. -->

> **Mode:** Fix & Report
> **Project:** {App/Project name}
> **RN version:** {x.y.z} · **Expo SDK:** {x.y.z | none}
> **Platforms:** {iOS | Android | both}
> **Scope:** {screens/actions analyzed and changed}
> **Build used for verdicts:** {Release preferred — debug numbers not representative}
> **Generated by:** react-native-performance-analyzer v0.0.1 · **Date:** {YYYY-MM-DD}

> **Mode statement:** Mode: Fix & Report — changes were applied per the approved permission grant.

## Summary

{2–4 sentences: what was analyzed, the real bottleneck (named), what changed, and the headline before/after result.}

## Issues Found

| # | Area | Issue (traced to code) | Severity |
|---|------|------------------------|----------|
| 1 | {Area} | {`file:line` → concrete, named cause} | {Critical/High/Medium/Low} |
| 2 | Lists | {`src/screens/Feed/FeedScreen.tsx:24` — inline `renderItem` + non-memoized row re-rendered every item on each interaction} | High |
| 3 | {Area} | {cause} | {Severity} |

## Files Changed

| File | Why |
|------|-----|
| {`src/...`} | {the specific fix it contains} |
| {`App.tsx`} | {deferred analytics/preload after first paint; removed blocking startup fetch} |

<!-- One row per changed file. No file listed without a "why". -->

## Fixes Applied

<!-- One block per issue, tied back to the Issues Found table by number. -->

### Issue {1} — {short name} ({Severity})

- **Change:** {what changed in code; e.g. "stabilized `renderItem` via `useCallback` and memoized `FeedCard`"}
- **File:** {`src/...`}
- **Behavior impact:** {none — identical behavior, same data/layout}
- **Outcome (before → after):** {measurement if available; otherwise mark "unverified until run"}

### Issue {2} — {short name} ({Severity})

- **Change:** {…}
- **File:** {…}
- **Behavior impact:** {…}
- **Outcome (before → after):** {…}

<!-- No new dependency introduced without stating why + the measurement that justified it (e.g. FlashList). -->

## Validation

| Check | Command | Result |
|-------|---------|--------|
| TypeScript | `npm run typecheck` (or `tsc --noEmit`) | {✅ pass | ⚠️ unverified until run} |
| Lint | `npm run lint` | {…} |
| Tests | `npm test` | {…} |
| Android release | `cd android && ./gradlew assembleRelease` | {…} |
| iOS | `cd ios && pod install` (+ Release scheme) | {…} |
| Profiler | {React DevTools / Hermes / Android Studio / Instruments} | {…} |
| {Custom measurement} | {exact command, e.g. `adb shell am start -W`} | {…} |

<!-- Any check not run MUST be listed with the exact command and marked "unverified until run". -->

## Manual Testing Steps

1. {Reproduce step — screen, action, device/build}
2. {Verify step — what "fixed" looks like}
3. {Regression step — business behavior unchanged}
4. {Low-end device check if applicable}

## Remaining Risks

- {Needs real-device / production-monitoring verification}
- {Assumption that could break the fix, e.g. variable row heights if `getItemLayout` was added}
- {Any fix still pending a release-build re-measure}

## Next Steps

1. {Recommendation, e.g. add cold-start/scroll metrics to CI or Sentry; re-run this analyzer after the next batch of feature work}

---

_End of report — react-native-performance-analyzer v0.0.1_
